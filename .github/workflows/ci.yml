name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  changes:
    name: Detect Changes
    runs-on: ubuntu-latest
    outputs:
      rust: ${{ steps.filter.outputs.rust }}
      frontend: ${{ steps.filter.outputs.frontend }}
      tauri: ${{ steps.filter.outputs.tauri }}
      workflows: ${{ steps.filter.outputs.workflows }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            rust:
              - 'russh-ssh/**'
              - 'russh-ssh-cli/**'
              - 'russh-client/src-tauri/**'
              - 'Cargo.toml'
              - 'Cargo.lock'
            frontend:
              - 'russh-client/src/**'
              - 'russh-client/package.json'
              - 'russh-client/pnpm-lock.yaml'
            tauri:
              - 'russh-client/**'
            workflows:
              - '.github/workflows/**'

  test:
    name: Test
    needs: changes
    uses: ./.github/workflows/_test.yml
    with:
      run-e2e: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' }}
      skip-rust: ${{ needs.changes.outputs.rust != 'true' && needs.changes.outputs.workflows != 'true' }}
      skip-frontend: ${{ needs.changes.outputs.frontend != 'true' && needs.changes.outputs.workflows != 'true' }}

  build-desktop:
    name: Desktop
    needs: [changes, test]
    if: needs.changes.outputs.tauri == 'true' || needs.changes.outputs.workflows == 'true' || github.event_name == 'workflow_dispatch'
    uses: ./.github/workflows/_build-desktop.yml
    secrets: inherit

  build-mobile:
    name: Mobile
    needs: [changes, test]
    if: needs.changes.outputs.tauri == 'true' || needs.changes.outputs.workflows == 'true' || github.event_name == 'workflow_dispatch'
    uses: ./.github/workflows/_build-mobile.yml
    secrets: inherit
    with:
      platform: both
